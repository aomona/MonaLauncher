# プロジェクト概要

MonaLauncher は、Minecraft Java EditionをOSのサンドボックス内で実行する開発中のランチャーです。Tauri 2 / Rust をバックエンド、React / TypeScript / Vite をフロントエンドに使用しています。WindowsはAppContainer、macOSはSeatbelt、Linuxはbubblewrap / seccompで、ゲームプロセス全体の権限を制限します。ゲームの通信は既定OFFで、インスタンス設定から変更できます。OSごとの対応・保証は同一ではありません。

# 最初に読むもの

- 環境構築と起動は [README.md](README.md)、作業別の参照先・実装の入口・検証コマンドは [docs/README.md](docs/README.md) を確認してください。詳細資料は変更対象に関係するものから読んでください。
- `CLAUDE.md` はこのファイルへの相対シンボリックリンクです。エージェント別に同じ指示をコピーせず、共通ルールはここへ集約してください。

# 作業方針

- 作業開始時にブランチと `git status --short` を確認し、既存の変更を保持してください。
- 既存実装と呼び出し元を確認してから変更してください。IPCや保存形式の変更ではTypeScript型・Rust型・呼び出し元・テストの対応を確認してください。
- 変更ごとに、内容に応じた確認を行ってから Git コミットを作成してください。
- コミットは意味のある変更単位に分け、変更内容が分かるメッセージを付けてください。
- 自分の作業に関係のない既存の変更はコミットに含めないでください。
- 完了時は変更内容、実行した検証と結果、未検証の範囲、コミットを報告してください。実装済み・モック検証済み・対象OSでの実行確認済みを区別してください。

# サンドボックス・認証

- 権限・起動・終了処理を変更する前に [共通サンドボックスポリシー](docs/sandbox-policy.md)、認証を変更する前に [Microsoft認証](docs/microsoft-auth.md) と [認証仲介の実装状況・計画](docs/auth-broker-plan.md) を確認してください。
- 未対応の権限要求やサンドボックスの起動失敗を、隔離なしの起動へフォールバックさせないでください。権限の入力検証・パス境界・子プロセスの後始末を維持してください。
- ゲームの通信許可とアカウント認証の仲介は別設定です。仲介失敗時に実トークン・署名秘密鍵をゲームへ直接渡す経路を追加しないでください。資格情報をログ・テスト成果物・コミットへ残さないでください。
- OS固有の変更は対象OSで検証し、実行できなかった場合はその制限を報告してください。別OSでの成功や過去の検証記録から、現在の動作・安全性を推定しないでください。

# デザイン実装

- 共通の対話UIは `@base-ui/react` の対応する部品を使用し、`src/components/` でデザイントークンに合わせて実装してください。既存の共通部品を優先して再利用してください。
- UI・CSS・コンポーネントを変更する前に [design/README.md](design/README.md) を読み、実装方法と検証手順に従ってください。
- 挙動・画面構成は [デザイン定義 v1.1](design/MonaLauncher_Design_Definition_v1.1.md) の該当章、数値は [デザイントークン v1.1](design/MonaLauncher_Design_Tokens_v1.1.json) を参照してください。既存画面の配色・装飾・寸法を新デザインの根拠にしないでください。
- Tailwind CSS v4 の設定は `src/styles/theme.generated.css` です。生成物を手編集せず、トークンまたは `scripts/generate-design-theme.mjs` を変更して `pnpm design:generate` を実行してください。
- 添付原本は参照資料として保持します。仕様変更を依頼された場合に限り更新し、ガイド・生成設定との整合性を確認してください。
- デザイン変更後は `pnpm check` と、変更範囲に対応するデザイン定義18章の画面・操作検証を行ってください。設定の検証だけで画面全体を実装済み・アクセシビリティ適合済みと扱わないでください。

# Reactアーキテクチャ

- Reactを変更する前に [フロントエンドアーキテクチャ](docs/frontend-architecture.md) を確認してください。
- ページは機能コンポーネントの組み立てを中心にし、状態・操作・意味上の責務で分割してください。行数を減らすだけのwrapperは作らないでください。
- 部品を追加する前に既存の同等部品を確認してください。画面固有の部品はfeature内に置き、共通ディレクトリには本当に再利用する部品だけを置いてください。
- 複雑な非UIロジックはhook・utility・domainへ移してください。実装後に複数の責務が蓄積した部品を再整理してください。
